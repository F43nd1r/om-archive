
solution {
    puzzle = "P085"
    name = "B AG 6T"
    arm(PISTON) {
        number = 1
        position = 3 to -2
        rotation = -2
        size = 2
    }
    glyph(BONDER) {
        position = 1 to -1
        rotation = 5
    }
    glyph(EQUILIBRIUM) {
        position = 3 to -3
        rotation = 0
    }
    io(INPUT) {
        index = 0
        position = 1 to 0
        rotation = 0
    }
    io(INFINITE) {
        index = 0
        position = -10 to -3
        rotation = 0
    }
    io(INPUT) {
        index = 1
        position = 3 to -4
        rotation = 0
    }
    track {
        position = 2 to -1
        positions = listOf(0 to 0, 1 to -1)
    }
    tape {
        parallel(
        {
            sequence(1) {
                grab()
                retract()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                rotateClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                back()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                forward()
                grab()
                retract()
                rotateClockwise()
                back()
                drop()
                rotateClockwise()
                grab()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                back()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                forward()
                grab()
                retract()
                rotateClockwise()
                back()
                drop()
                rotateClockwise()
                grab()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                back()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                forward()
                grab()
                retract()
                rotateClockwise()
                back()
                drop()
                rotateClockwise()
                grab()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                back()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                forward()
                grab()
                retract()
                rotateClockwise()
                back()
                drop()
                rotateClockwise()
                grab()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                back()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                forward()
                grab()
                retract()
                rotateClockwise()
                back()
                drop()
                rotateClockwise()
                grab()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                retract()
                extend()
                rotateClockwise()
                reset()
            }
        }
        )
    }
}
