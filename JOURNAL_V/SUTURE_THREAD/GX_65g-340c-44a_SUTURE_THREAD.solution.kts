
solution {
    puzzle = "P085"
    name = "B ACX 6T"
    arm(PISTON) {
        number = 1
        position = 3 to -2
        rotation = -2
        size = 2
    }
    glyph(BONDER) {
        position = 1 to -1
        rotation = 5
    }
    io(INPUT) {
        index = 0
        position = 1 to 0
        rotation = 0
    }
    io(INFINITE) {
        index = 0
        position = -10 to -3
        rotation = 0
    }
    io(INPUT) {
        index = 1
        position = 3 to -4
        rotation = 0
    }
    track {
        position = 2 to -1
        positions = listOf(0 to 0, 1 to -1, 1 to -2)
    }
    tape {
        parallel(
        {
            sequence(1) {
                grab()
                retract()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                rotateClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                forward()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                retract()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                back()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                back()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                forward()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                forward()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                back()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                back()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                forward()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                forward()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                back()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                back()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                forward()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                forward()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                back()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                back()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                forward()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                forward()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                back()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                back()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                forward()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                forward()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                back()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                retract()
                extend()
                rotateClockwise()
                reset()
            }
        }
        )
    }
}
