
solution {
    puzzle = "P078"
    name = "B GC NOGIF"
    arm(ARM1) {
        number = 1
        position = -2 to 1
        rotation = 2
        size = 2
    }
    glyph(BONDER) {
        position = 0 to -1
        rotation = 2
    }
    glyph(UNBONDER) {
        position = -2 to 2
        rotation = 1
    }
    glyph(ANIMISMUS) {
        position = 0 to 1
        rotation = 3
    }
    io(OUTPUT) {
        index = 0
        position = 0 to -2
        rotation = 0
    }
    io(INPUT) {
        index = 0
        position = -3 to 3
        rotation = 3
    }
    track {
        position = -3 to 1
        positions = listOf(0 to 0, 0 to 1, 1 to 0, 1 to -1)
    }
    conduit {
        id = 100
        position = 2 to 2
        rotation = 0
    }
    conduit {
        id = 100
        position = -4 to 0
        rotation = 2
    }
    tape {
        parallel(
        {
            sequence(1) {
                grab()
                rotateClockwise()
                pivotClockwise()
                rotateClockwise()
                rotateClockwise()
                back()
                drop()
                forward()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                back()
                grab()
                rotateClockwise()
                pivotClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                pivotClockwise()
                drop()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                back()
                back()
                rotateClockwise()
                drop()
                forward()
                grab()
                back()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                forward()
                pivotCounterClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                forward()
                drop()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                back()
                rotateClockwise()
                back()
                rotateClockwise()
                forward()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                pivotClockwise()
                rotateClockwise()
                back()
                rotateClockwise()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                back()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                grab()
                forward()
                rotateCounterClockwise()
                drop()
                back()
                grab()
                pivotCounterClockwise()
                forward()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                back()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                reset()
                grab()
                rotateClockwise()
                pivotClockwise()
                rotateClockwise()
                rotateClockwise()
                back()
                drop()
                forward()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                back()
                grab()
                rotateClockwise()
                pivotClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                pivotClockwise()
                drop()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                back()
                back()
                rotateClockwise()
                drop()
                forward()
                grab()
                back()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                forward()
                pivotCounterClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                forward()
                drop()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                back()
                rotateClockwise()
                back()
                rotateClockwise()
                forward()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                pivotClockwise()
                rotateClockwise()
                back()
                rotateClockwise()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                back()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                grab()
                forward()
                rotateCounterClockwise()
                drop()
                back()
                grab()
                pivotCounterClockwise()
                forward()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                back()
                back()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                pivotClockwise()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                grab()
                back()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                reset()
                grab()
                rotateClockwise()
                pivotClockwise()
                rotateClockwise()
                back()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                back()
                back()
                rotateClockwise()
                drop()
                forward()
                grab()
                back()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                forward()
                pivotCounterClockwise()
                forward()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                grab()
                rotateClockwise()
                forward()
                drop()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                back()
                rotateClockwise()
                back()
                rotateClockwise()
                forward()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                pivotClockwise()
                rotateClockwise()
                back()
                rotateClockwise()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                back()
                drop()
                forward()
                forward()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                back()
                grab()
                pivotCounterClockwise()
                forward()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                back()
                back()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                reset()
                grab()
                rotateClockwise()
                pivotClockwise()
                rotateClockwise()
                back()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                back()
                back()
                rotateClockwise()
                drop()
                forward()
                grab()
                back()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                forward()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                grab()
                rotateClockwise()
                forward()
                drop()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                back()
                rotateClockwise()
                back()
                rotateClockwise()
                forward()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                pivotClockwise()
                rotateClockwise()
                back()
                rotateClockwise()
                rotateCounterClockwise()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                back()
                drop()
                forward()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                forward()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                back()
                back()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotClockwise()
                pivotClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                back()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                reset()
                grab()
                rotateClockwise()
                pivotClockwise()
                rotateClockwise()
                back()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                back()
                back()
                rotateClockwise()
                drop()
                forward()
                grab()
                back()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                forward()
                pivotCounterClockwise()
                drop()
                back()
                grab()
                back()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                back()
                back()
                grab()
                rotateClockwise()
                back()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                back()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                pivotClockwise()
                rotateClockwise()
                back()
                rotateClockwise()
                pivotCounterClockwise()
                forward()
                drop()
                back()
                back()
                grab()
                forward()
                drop()
                forward()
                grab()
                rotateCounterClockwise()
                forward()
                forward()
                drop()
                back()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                forward()
                forward()
                rotateCounterClockwise()
                grab()
                back()
                drop()
                rotateClockwise()
                forward()
                grab()
                pivotCounterClockwise()
                forward()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                back()
                rotateClockwise()
                drop()
                rotateClockwise()
                reset()
                grab()
                rotateClockwise()
                pivotClockwise()
                rotateClockwise()
                rotateClockwise()
                back()
                drop()
                back()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                back()
                back()
                rotateClockwise()
                drop()
                forward()
                grab()
                rotateClockwise()
                back()
                rotateCounterClockwise()
                forward()
                forward()
                pivotCounterClockwise()
                drop()
                forward()
                grab()
                forward()
                forward()
                rotateClockwise()
                forward()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                back()
                rotateClockwise()
                rotateClockwise()
                pivotClockwise()
                pivotClockwise()
                forward()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                pivotClockwise()
                rotateClockwise()
                rotateClockwise()
                back()
                drop()
                back()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                forward()
                drop()
                back()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                pivotClockwise()
                rotateClockwise()
                rotateClockwise()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                back()
                back()
                grab()
                forward()
                drop()
                rotateClockwise()
                back()
                grab()
                forward()
                pivotCounterClockwise()
                back()
                pivotCounterClockwise()
                drop()
                back()
                grab()
                rotateCounterClockwise()
                drop()
                forward()
                forward()
                rotateClockwise()
                grab()
                pivotClockwise()
                drop()
                forward()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                rotateClockwise()
                drop()
                back()
                grab()
                pivotCounterClockwise()
                forward()
                forward()
                drop()
            }
        }
        )
    }
}
