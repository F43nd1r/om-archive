
solution {
    puzzle = "P108"
    name = "B AA"
    arm(PISTON) {
        number = 1
        position = 4 to -4
        rotation = 11
        size = 1
    }
    arm(PISTON) {
        number = 2
        position = 2 to -5
        rotation = 9
        size = 1
    }
    glyph(BONDER) {
        position = 1 to -2
        rotation = 5
    }
    glyph(UNBONDER) {
        position = 3 to -3
        rotation = 6
    }
    glyph(CALCIFICATION) {
        position = 1 to 1
        rotation = 0
    }
    glyph(PROJECTION) {
        position = 1 to -4
        rotation = 19
    }
    glyph(ANIMISMUS) {
        position = 3 to -5
        rotation = 19
    }
    glyph(DISPOSAL) {
        position = -1 to -2
        rotation = 0
    }
    glyph(EQUILIBRIUM) {
        position = -2 to 0
        rotation = 0
    }
    io(OUTPUT) {
        index = 0
        position = 1 to 0
        rotation = 6
    }
    io(INPUT) {
        index = 1
        position = 1 to -5
        rotation = 8
    }
    io(INPUT) {
        index = 2
        position = 5 to -5
        rotation = 0
    }
    tape {
        parallel(
        {
            sequence(1) {
                grab()
                rotateClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                rotateClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                wait(5)
                retract()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                wait(3)
                grab()
                retract()
                rotateClockwise()
                pivotClockwise()
                extend()
                drop()
                retract()
                wait(3)
                grab()
                extend()
                pivotClockwise()
                extend()
                retract()
                pivotCounterClockwise()
                retract()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                wait(1)
                grab()
                extend()
                retract()
                retract()
                drop()
                extend()
                grab()
                retract()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                wait(4)
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                wait(8)
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                wait(2)
                grab()
                retract()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                extend()
                pivotClockwise()
                drop()
                retract()
                grab()
                extend()
                pivotClockwise()
                extend()
                retract()
                pivotCounterClockwise()
                retract()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                wait(1)
                grab()
                extend()
                retract()
                drop()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                retract()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                wait(1)
                grab()
                pivotCounterClockwise()
                wait(3)
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                extend()
                wait(8)
                grab()
                retract()
                rotateClockwise()
                pivotClockwise()
                pivotClockwise()
                drop()
                retract()
                wait(1)
                grab()
                extend()
                pivotClockwise()
                extend()
                retract()
                pivotCounterClockwise()
                retract()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                wait(1)
                grab()
                extend()
                retract()
                retract()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                retract()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                wait(2)
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                wait(1)
                grab()
                retract()
                rotateClockwise()
                pivotClockwise()
                pivotClockwise()
                retract()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                extend()
                drop()
                extend()
                grab()
                retract()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                wait(1)
                grab()
                extend()
                retract()
                pivotClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                wait(1)
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                retract()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                wait(7)
                grab()
                rotateClockwise()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                pivotClockwise()
                drop()
                retract()
                rotateClockwise()
                grab()
                pivotClockwise()
                drop()
                wait(3)
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotCounterClockwise()
                retract()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                grab()
                retract()
                drop()
                wait(1)
                grab()
                extend()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                drop()
                rotateClockwise()
                extend()
                wait(2)
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                grab()
                extend()
                extend()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                retract()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                rotateClockwise()
                pivotClockwise()
                pivotClockwise()
                retract()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                extend()
                wait(3)
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotCounterClockwise()
                wait(8)
                pivotClockwise()
                drop()
                wait(3)
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                wait(3)
                grab()
                pivotClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                drop()
                wait(1)
                grab()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                extend()
                retract()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                reset()
            }
        }
        , 
        {
            sequence(2) {
                wait(1)
                grab()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                drop()
                retract()
                wait(3)
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                wait(2)
                grab()
                pivotClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                extend()
                grab()
                rotateCounterClockwise()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                drop()
                wait(6)
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                grab()
                extend()
                drop()
                wait(3)
                grab()
                pivotCounterClockwise()
                drop()
                wait(2)
                grab()
                retract()
                retract()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                extend()
                drop()
                retract()
                retract()
                grab()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                grab()
                rotateClockwise()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                wait(2)
                grab()
                pivotClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                drop()
                wait(7)
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                grab()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                wait(4)
                grab()
                pivotClockwise()
                drop()
                retract()
                grab()
                extend()
                pivotClockwise()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                extend()
                drop()
                retract()
                retract()
                grab()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                drop()
                wait(6)
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                grab()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                extend()
                grab()
                rotateCounterClockwise()
                drop()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                extend()
                drop()
                retract()
                retract()
                grab()
                rotateClockwise()
                drop()
                extend()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                extend()
                grab()
                rotateCounterClockwise()
                drop()
                extend()
                wait(3)
                grab()
                retract()
                wait(3)
                extend()
                rotateCounterClockwise()
                drop()
                retract()
                wait(2)
                grab()
                pivotClockwise()
                retract()
                drop()
                rotateClockwise()
                wait(2)
                grab()
                extend()
                pivotClockwise()
                pivotCounterClockwise()
                pivotCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                pivotClockwise()
                pivotClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                wait(4)
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                extend()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                retract()
                rotateClockwise()
                drop()
                wait(2)
                extend()
                grab()
                pivotClockwise()
                extend()
                drop()
                retract()
                grab()
                pivotClockwise()
                drop()
                retract()
                wait(3)
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                rotateClockwise()
                wait(8)
                grab()
                retract()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                extend()
                retract()
                pivotCounterClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                wait(1)
                grab()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                wait(2)
                grab()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                extend()
                wait(2)
                grab()
                rotateCounterClockwise()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                extend()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                retract()
                rotateClockwise()
                pivotClockwise()
                drop()
                extend()
                grab()
                pivotClockwise()
                extend()
                drop()
                retract()
                grab()
                pivotClockwise()
                drop()
                retract()
                wait(3)
                grab()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                extend()
                extend()
                wait(4)
                grab()
                retract()
                drop()
                retract()
                wait(6)
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                reset()
            }
        }
        )
    }
}
