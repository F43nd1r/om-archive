
solution {
    puzzle = "P070"
    name = "B GC 4T"
    arm(ARM1) {
        number = 1
        position = 0 to -2
        rotation = -2
        size = 2
    }
    glyph(BONDER) {
        position = -3 to 1
        rotation = -6
    }
    glyph(CALCIFICATION) {
        position = 0 to 1
        rotation = 0
    }
    glyph(DISPERSION) {
        position = -2 to -2
        rotation = -4
    }
    io(INPUT) {
        index = 0
        position = 0 to -4
        rotation = 0
    }
    io(OUTPUT) {
        index = 0
        position = 2 to -1
        rotation = -1
    }
    track {
        position = -1 to -1
        positions = listOf(0 to 0, 1 to 0, 2 to -1, 1 to -1)
    }
    tape {
        parallel(
        {
            sequence(1) {
                grab()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                rotateClockwise()
                drop()
                forward()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                forward()
                forward()
                forward()
                pivotClockwise()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                pivotCounterClockwise()
                pivotCounterClockwise()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                back()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                forward()
                rotateCounterClockwise()
                reset()
                grab()
                rotateClockwise()
                drop()
                forward()
                grab()
                rotateClockwise()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                forward()
                rotateClockwise()
                back()
                pivotCounterClockwise()
                drop()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                rotateClockwise()
                forward()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                drop()
                forward()
                forward()
                rotateCounterClockwise()
                reset()
                grab()
                rotateClockwise()
                drop()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                rotateCounterClockwise()
                drop()
                forward()
                rotateCounterClockwise()
                grab()
                forward()
                forward()
                rotateClockwise()
                back()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                back()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                back()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                back()
                rotateClockwise()
                back()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                reset()
                grab()
                rotateClockwise()
                drop()
                forward()
                grab()
                rotateClockwise()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                forward()
                rotateClockwise()
                forward()
                rotateClockwise()
                forward()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotClockwise()
                drop()
                forward()
                forward()
                rotateCounterClockwise()
                reset()
                grab()
                rotateClockwise()
                drop()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                pivotCounterClockwise()
                pivotCounterClockwise()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                back()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                back()
                rotateClockwise()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                forward()
                rotateClockwise()
                back()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                reset()
                grab()
                rotateClockwise()
                drop()
                forward()
                grab()
                rotateClockwise()
                forward()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                forward()
                forward()
                rotateClockwise()
                forward()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                forward()
                rotateCounterClockwise()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                rotateClockwise()
                back()
                back()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                drop()
                forward()
                grab()
                rotateClockwise()
                back()
                pivotCounterClockwise()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                forward()
                forward()
                rotateClockwise()
                drop()
                forward()
                grab()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                drop()
                forward()
                grab()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                rotateClockwise()
                forward()
                rotateClockwise()
                forward()
                drop()
                forward()
                reset()
            }
        }
        )
    }
}
