
solution {
    puzzle = "w2501727889"
    name = "NEW SOLUTION 2"
    arm(PISTON) {
        number = 1
        position = 3 to -1
        rotation = 4
        size = 1
    }
    glyph(BONDER) {
        position = 4 to 2
        rotation = 0
    }
    glyph(BONDER) {
        position = 3 to 1
        rotation = -1
    }
    glyph(BONDER) {
        position = 0 to 2
        rotation = 0
    }
    glyph(CALCIFICATION) {
        position = 1 to -1
        rotation = 0
    }
    glyph(ANIMISMUS) {
        position = 1 to 0
        rotation = 1
    }
    io(INFINITE) {
        index = 0
        position = 2 to 1
        rotation = 0
    }
    io(INPUT) {
        index = 0
        position = 4 to -2
        rotation = 0
    }
    io(INPUT) {
        index = 1
        position = 3 to -2
        rotation = 0
    }
    io(OUTPUT) {
        index = 1
        position = 5 to 0
        rotation = 0
    }
    track {
        position = 2 to -1
        positions = listOf(0 to 0, 1 to 0, 2 to 0, 3 to 0)
    }
    tape {
        parallel(
        {
            sequence(1) {
                wait(1)
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                drop()
                extend()
                grab()
                forward()
                rotateClockwise()
                retract()
                forward()
                forward()
                drop()
                back()
                back()
                rotateCounterClockwise()
                grab()
                forward()
                rotateClockwise()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                drop()
                extend()
                grab()
                forward()
                forward()
                rotateClockwise()
                forward()
                retract()
                drop()
                back()
                back()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                pivotCounterClockwise()
                back()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                pivotCounterClockwise()
                pivotCounterClockwise()
                forward()
                drop()
                retract()
                back()
                back()
                back()
                rotateClockwise()
                extend()
                grab()
                forward()
                forward()
                forward()
                drop()
                retract()
                retract()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                back()
                back()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                forward()
                extend()
                pivotClockwise()
                drop()
                retract()
                retract()
                back()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                pivotCounterClockwise()
                extend()
                drop()
                retract()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                forward()
                forward()
                drop()
                back()
                extend()
                grab()
                pivotClockwise()
                forward()
                drop()
                back()
                back()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                drop()
                back()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                retract()
                pivotClockwise()
                drop()
                retract()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                drop()
                forward()
                grab()
                extend()
                extend()
                retract()
                drop()
                back()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                forward()
                forward()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                forward()
                drop()
                back()
                back()
                back()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                forward()
                forward()
                rotateClockwise()
                drop()
                back()
                back()
                extend()
                extend()
                grab()
                forward()
                forward()
                forward()
                drop()
                retract()
                retract()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                back()
                back()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                forward()
                extend()
                pivotClockwise()
                drop()
                retract()
                retract()
                back()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                pivotCounterClockwise()
                extend()
                drop()
                retract()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                forward()
                forward()
                drop()
                back()
                extend()
                grab()
                pivotClockwise()
                forward()
                drop()
                back()
                back()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                drop()
                back()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                retract()
                pivotClockwise()
                drop()
                retract()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                drop()
                forward()
                grab()
                extend()
                extend()
                retract()
                drop()
                back()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                forward()
                forward()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                forward()
                drop()
                back()
                back()
                back()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                forward()
                forward()
                rotateClockwise()
                drop()
                back()
                back()
                extend()
                extend()
                grab()
                forward()
                forward()
                forward()
                drop()
                retract()
                retract()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                back()
                back()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                forward()
                extend()
                pivotClockwise()
                drop()
                retract()
                retract()
                back()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                pivotCounterClockwise()
                extend()
                drop()
                retract()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                forward()
                forward()
                drop()
                back()
                extend()
                grab()
                pivotClockwise()
                forward()
                drop()
                back()
                back()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                drop()
                back()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                retract()
                pivotClockwise()
                drop()
                retract()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                drop()
                forward()
                grab()
                extend()
                extend()
                retract()
                drop()
                back()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                forward()
                forward()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                forward()
                drop()
                back()
                back()
                back()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                forward()
                forward()
                rotateClockwise()
                drop()
                back()
                back()
                extend()
                extend()
                grab()
                forward()
                forward()
                forward()
                drop()
                retract()
                retract()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                back()
                back()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                forward()
                extend()
                pivotClockwise()
                drop()
                retract()
                retract()
                back()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                pivotCounterClockwise()
                extend()
                drop()
                retract()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                forward()
                forward()
                drop()
                back()
                extend()
                grab()
                pivotClockwise()
                forward()
                drop()
                back()
                back()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                drop()
                back()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                retract()
                pivotClockwise()
                drop()
                retract()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                drop()
                forward()
                grab()
                extend()
                extend()
                retract()
                drop()
                back()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                forward()
                forward()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                forward()
                drop()
                back()
                back()
                back()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                forward()
                forward()
                rotateClockwise()
                drop()
                back()
                back()
                extend()
                extend()
                grab()
                forward()
                forward()
                forward()
                drop()
                retract()
                retract()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                back()
                back()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                forward()
                extend()
                pivotClockwise()
                drop()
                retract()
                retract()
                back()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                pivotCounterClockwise()
                extend()
                drop()
                retract()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                forward()
                forward()
                drop()
                back()
                extend()
                grab()
                pivotClockwise()
                forward()
                drop()
                back()
                back()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                drop()
                back()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                retract()
                pivotClockwise()
                drop()
                retract()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                drop()
                forward()
                grab()
                extend()
                extend()
                retract()
                drop()
                back()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                forward()
                forward()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                forward()
                drop()
                back()
                back()
                back()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                forward()
                forward()
                rotateClockwise()
                drop()
                back()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                pivotCounterClockwise()
                back()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                extend()
                extend()
                pivotCounterClockwise()
                back()
                drop()
                retract()
                forward()
                rotateClockwise()
                grab()
                back()
                back()
                forward()
                forward()
            }
        }
        )
    }
}
