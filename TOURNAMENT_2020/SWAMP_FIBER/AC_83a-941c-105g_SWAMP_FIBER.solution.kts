
solution {
    puzzle = "w2501727889"
    name = "B AA 6T"
    arm(PISTON) {
        number = 1
        position = -4 to 1
        rotation = -3
        size = 1
    }
    glyph(BONDER) {
        position = -1 to -1
        rotation = -1
    }
    glyph(BONDER) {
        position = -4 to 0
        rotation = 0
    }
    glyph(CALCIFICATION) {
        position = -4 to -2
        rotation = 0
    }
    glyph(ANIMISMUS) {
        position = -3 to -1
        rotation = -2
    }
    glyph(EQUILIBRIUM) {
        position = 11 to -1
        rotation = 0
    }
    io(INPUT) {
        index = 0
        position = -5 to 1
        rotation = 0
    }
    io(INFINITE) {
        index = 0
        position = -2 to -1
        rotation = 0
    }
    io(INPUT) {
        index = 1
        position = -1 to 1
        rotation = 0
    }
    io(OUTPUT) {
        index = 1
        position = 1 to -2
        rotation = 0
    }
    track {
        position = -2 to 1
        positions = listOf(0 to 0, -1 to 0, -2 to 0)
    }
    tape {
        parallel(
        {
            sequence(1) {
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                retract()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                back()
                grab()
                retract()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                back()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                forward()
                rotateClockwise()
                rotateClockwise()
                extend()
                retract()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                back()
                grab()
                retract()
                retract()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                rotateClockwise()
                forward()
                forward()
                grab()
                retract()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                back()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                retract()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                back()
                grab()
                retract()
                forward()
                rotateCounterClockwise()
                extend()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                retract()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                back()
                grab()
                retract()
                forward()
                retract()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                forward()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                back()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                forward()
                rotateClockwise()
                rotateClockwise()
                drop()
                back()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                forward()
                extend()
                grab()
                retract()
                drop()
                back()
                grab()
                extend()
                pivotCounterClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                back()
                rotateClockwise()
                back()
                retract()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                grab()
                forward()
                drop()
                back()
                rotateClockwise()
                grab()
                forward()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                back()
                back()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                back()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                retract()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                back()
                grab()
                retract()
                forward()
                rotateCounterClockwise()
                extend()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                retract()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                back()
                grab()
                retract()
                forward()
                retract()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                forward()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                back()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                forward()
                rotateClockwise()
                rotateClockwise()
                drop()
                back()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                forward()
                extend()
                grab()
                retract()
                drop()
                back()
                grab()
                extend()
                pivotCounterClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                back()
                rotateClockwise()
                back()
                retract()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                grab()
                forward()
                drop()
                back()
                rotateClockwise()
                grab()
                forward()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                back()
                back()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                back()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                retract()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                back()
                grab()
                retract()
                forward()
                rotateCounterClockwise()
                extend()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                retract()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                back()
                grab()
                retract()
                forward()
                retract()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                forward()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                back()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                forward()
                rotateClockwise()
                rotateClockwise()
                drop()
                back()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                forward()
                extend()
                grab()
                retract()
                drop()
                back()
                grab()
                extend()
                pivotCounterClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                back()
                rotateClockwise()
                back()
                retract()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                grab()
                forward()
                drop()
                back()
                rotateClockwise()
                grab()
                forward()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                back()
                back()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                back()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                retract()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                back()
                grab()
                retract()
                forward()
                rotateCounterClockwise()
                extend()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                retract()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                back()
                grab()
                retract()
                forward()
                retract()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                forward()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                back()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                forward()
                rotateClockwise()
                rotateClockwise()
                drop()
                back()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                forward()
                extend()
                grab()
                retract()
                drop()
                back()
                grab()
                extend()
                pivotCounterClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                back()
                rotateClockwise()
                back()
                retract()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                grab()
                forward()
                drop()
                back()
                rotateClockwise()
                grab()
                forward()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                back()
                back()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                back()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                retract()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                back()
                grab()
                retract()
                forward()
                rotateCounterClockwise()
                extend()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                retract()
                back()
                drop()
                retract()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                back()
                grab()
                retract()
                forward()
                retract()
                rotateCounterClockwise()
                extend()
                extend()
                back()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                forward()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                forward()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                back()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                forward()
                rotateClockwise()
                rotateClockwise()
                drop()
                back()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                forward()
                extend()
                grab()
                retract()
                drop()
                back()
                grab()
                extend()
                pivotCounterClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                back()
                rotateClockwise()
                back()
                retract()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                grab()
                forward()
                drop()
                back()
                rotateClockwise()
                grab()
                forward()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                back()
                back()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                back()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                forward()
                forward()
                rotateClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                back()
                grab()
                forward()
                forward()
                rotateClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                back()
                grab()
                forward()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                forward()
                back()
                back()
                extend()
                rotateCounterClockwise()
                rotateCounterClockwise()
                reset()
            }
        }
        )
    }
}
