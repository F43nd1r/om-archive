
solution {
    puzzle = "w2501727889"
    name = "B GC 6T"
    arm(ARM1) {
        number = 1
        position = -5 to 1
        rotation = 2
        size = 2
    }
    glyph(BONDER) {
        position = -2 to 1
        rotation = -1
    }
    glyph(CALCIFICATION) {
        position = -6 to 2
        rotation = 0
    }
    glyph(ANIMISMUS) {
        position = -5 to 2
        rotation = 7
    }
    glyph(EQUILIBRIUM) {
        position = 6 to 1
        rotation = 0
    }
    io(INPUT) {
        index = 0
        position = -7 to 3
        rotation = 0
    }
    io(INFINITE) {
        index = 0
        position = -3 to 1
        rotation = 0
    }
    io(INPUT) {
        index = 1
        position = -3 to -1
        rotation = 0
    }
    io(OUTPUT) {
        index = 1
        position = -4 to 3
        rotation = 0
    }
    track {
        position = -4 to 0
        positions = listOf(-1 to 1, 0 to 1, 1 to 0, 0 to 0)
    }
    tape {
        parallel(
        {
            sequence(1) {
                grab()
                back()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                back()
                drop()
                back()
                grab()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                back()
                drop()
                back()
                grab()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                rotateClockwise()
                forward()
                forward()
                forward()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotClockwise()
                forward()
                drop()
                forward()
                forward()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                forward()
                forward()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                back()
                grab()
                forward()
                forward()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                reset()
                grab()
                back()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                back()
                drop()
                back()
                grab()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                forward()
                rotateClockwise()
                forward()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                back()
                drop()
                back()
                grab()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                rotateClockwise()
                forward()
                forward()
                forward()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotClockwise()
                forward()
                drop()
                forward()
                forward()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                forward()
                forward()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                back()
                grab()
                forward()
                forward()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                reset()
                grab()
                back()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                back()
                drop()
                back()
                grab()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                forward()
                rotateClockwise()
                forward()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                back()
                drop()
                back()
                grab()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                rotateClockwise()
                forward()
                forward()
                forward()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotClockwise()
                forward()
                drop()
                forward()
                forward()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                forward()
                forward()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                back()
                grab()
                forward()
                forward()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                reset()
                grab()
                back()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                back()
                drop()
                back()
                grab()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                forward()
                rotateClockwise()
                forward()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                back()
                drop()
                back()
                grab()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                rotateClockwise()
                forward()
                forward()
                forward()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotClockwise()
                forward()
                drop()
                forward()
                forward()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                forward()
                forward()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                back()
                grab()
                forward()
                forward()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                reset()
                grab()
                back()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                back()
                drop()
                back()
                grab()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                forward()
                rotateClockwise()
                forward()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                back()
                drop()
                back()
                grab()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                rotateClockwise()
                forward()
                forward()
                forward()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotClockwise()
                forward()
                drop()
                forward()
                forward()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                forward()
                forward()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                back()
                grab()
                forward()
                forward()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                reset()
                grab()
                back()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                back()
                drop()
                back()
                grab()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                forward()
                rotateClockwise()
                forward()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                back()
                drop()
                back()
                grab()
                rotateClockwise()
                drop()
                back()
                back()
                grab()
                rotateClockwise()
                forward()
                forward()
                forward()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotClockwise()
                forward()
                drop()
                forward()
                forward()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                forward()
                forward()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                back()
                grab()
                forward()
                forward()
                rotateCounterClockwise()
                forward()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                reset()
            }
        }
        )
    }
}
