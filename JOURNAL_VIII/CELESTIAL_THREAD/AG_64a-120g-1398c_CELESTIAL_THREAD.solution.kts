
solution {
    puzzle = "P101"
    name = "B AA 6T"
    arm(PISTON) {
        number = 1
        position = 4 to -2
        rotation = -1
        size = 1
    }
    glyph(BONDER) {
        position = 3 to -3
        rotation = 1
    }
    glyph(UNBONDER) {
        position = 3 to -4
        rotation = 6
    }
    glyph(CALCIFICATION) {
        position = 5 to -4
        rotation = 0
    }
    glyph(PROJECTION) {
        position = 4 to -1
        rotation = 2
    }
    glyph(PURIFICATION) {
        position = 2 to 0
        rotation = 4
    }
    io(INFINITE) {
        index = 0
        position = -10 to -2
        rotation = 0
    }
    io(INPUT) {
        index = 0
        position = 5 to -2
        rotation = 10
    }
    io(INPUT) {
        index = 1
        position = 1 to 0
        rotation = 0
    }
    track {
        position = 4 to -3
        positions = listOf(0 to 0, 0 to 1)
    }
    tape {
        parallel(
        {
            sequence(1) {
                grab()
                rotateClockwise()
                rotateClockwise()
                back()
                drop()
                rotateClockwise()
                extend()
                extend()
                grab()
                retract()
                forward()
                drop()
                back()
                extend()
                grab()
                retract()
                drop()
                retract()
                forward()
                grab()
                back()
                rotateCounterClockwise()
                extend()
                rotateClockwise()
                forward()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                pivotClockwise()
                drop()
                retract()
                forward()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                grab()
                pivotCounterClockwise()
                pivotCounterClockwise()
                retract()
                retract()
                rotateCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                forward()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                back()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                grab()
                extend()
                drop()
                retract()
                back()
                rotateClockwise()
                extend()
                grab()
                retract()
                forward()
                drop()
                back()
                extend()
                grab()
                retract()
                drop()
                retract()
                forward()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                forward()
                drop()
                back()
                grab()
                forward()
                drop()
                rotateClockwise()
                rotateClockwise()
                back()
                grab()
                pivotCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                extend()
                pivotCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                pivotCounterClockwise()
                rotateClockwise()
                pivotCounterClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                pivotClockwise()
                rotateCounterClockwise()
                extend()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                rotateCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                retract()
                extend()
                extend()
                drop()
                retract()
                back()
                grab()
                extend()
                drop()
                retract()
                rotateClockwise()
                extend()
                grab()
                retract()
                forward()
                drop()
                back()
                extend()
                grab()
                retract()
                drop()
                retract()
                forward()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                forward()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                back()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                forward()
                grab()
                back()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                back()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                pivotClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                extend()
                drop()
                retract()
                forward()
                grab()
                extend()
                extend()
                drop()
                retract()
                back()
                rotateClockwise()
                extend()
                grab()
                retract()
                forward()
                drop()
                back()
                extend()
                grab()
                retract()
                drop()
                retract()
                forward()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                forward()
                drop()
                back()
                grab()
                forward()
                drop()
                rotateClockwise()
                rotateClockwise()
                back()
                grab()
                pivotCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                extend()
                pivotCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                pivotCounterClockwise()
                rotateClockwise()
                pivotCounterClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                pivotClockwise()
                rotateCounterClockwise()
                extend()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                rotateCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                retract()
                extend()
                extend()
                drop()
                retract()
                back()
                grab()
                extend()
                drop()
                retract()
                rotateClockwise()
                extend()
                grab()
                retract()
                forward()
                drop()
                back()
                extend()
                grab()
                retract()
                drop()
                retract()
                forward()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                forward()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                back()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                forward()
                grab()
                back()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                back()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                pivotClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                extend()
                drop()
                retract()
                forward()
                grab()
                extend()
                extend()
                drop()
                retract()
                back()
                rotateClockwise()
                extend()
                grab()
                retract()
                forward()
                drop()
                back()
                extend()
                grab()
                retract()
                drop()
                retract()
                forward()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                forward()
                drop()
                back()
                grab()
                forward()
                drop()
                rotateClockwise()
                rotateClockwise()
                back()
                grab()
                pivotCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                extend()
                pivotCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                pivotCounterClockwise()
                rotateClockwise()
                pivotCounterClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                pivotClockwise()
                rotateCounterClockwise()
                extend()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                rotateCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                retract()
                extend()
                extend()
                drop()
                retract()
                back()
                grab()
                extend()
                drop()
                retract()
                rotateClockwise()
                extend()
                grab()
                retract()
                forward()
                drop()
                back()
                extend()
                grab()
                retract()
                drop()
                retract()
                forward()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                forward()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                back()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                forward()
                grab()
                back()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                back()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                pivotClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                extend()
                drop()
                retract()
                forward()
                grab()
                extend()
                extend()
                drop()
                retract()
                back()
                rotateClockwise()
                extend()
                grab()
                retract()
                forward()
                drop()
                back()
                extend()
                grab()
                retract()
                drop()
                retract()
                forward()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                forward()
                drop()
                back()
                grab()
                forward()
                drop()
                rotateClockwise()
                rotateClockwise()
                back()
                grab()
                pivotCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                extend()
                pivotCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                pivotCounterClockwise()
                rotateClockwise()
                pivotCounterClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                pivotClockwise()
                rotateCounterClockwise()
                extend()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                rotateCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                retract()
                extend()
                extend()
                drop()
                retract()
                back()
                grab()
                extend()
                drop()
                retract()
                rotateClockwise()
                extend()
                grab()
                retract()
                forward()
                drop()
                back()
                extend()
                grab()
                retract()
                drop()
                retract()
                forward()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                forward()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                back()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                forward()
                grab()
                back()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                back()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                pivotClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                extend()
                drop()
                retract()
                forward()
                grab()
                extend()
                extend()
                drop()
                retract()
                back()
                rotateClockwise()
                extend()
                grab()
                retract()
                forward()
                drop()
                back()
                extend()
                grab()
                retract()
                drop()
                retract()
                forward()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                forward()
                drop()
                back()
                grab()
                forward()
                drop()
                rotateClockwise()
                rotateClockwise()
                back()
                grab()
                pivotCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                extend()
                pivotCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                pivotCounterClockwise()
                rotateClockwise()
                pivotCounterClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                pivotClockwise()
                rotateCounterClockwise()
                extend()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                rotateCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                retract()
                extend()
                extend()
                drop()
                retract()
                back()
                grab()
                extend()
                drop()
                retract()
                rotateClockwise()
                extend()
                grab()
                retract()
                forward()
                drop()
                back()
                extend()
                grab()
                retract()
                drop()
                retract()
                forward()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                forward()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                back()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                forward()
                grab()
                back()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                back()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                pivotClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                extend()
                drop()
                retract()
                forward()
                grab()
                extend()
                extend()
                drop()
                retract()
                back()
                rotateClockwise()
                extend()
                grab()
                retract()
                forward()
                drop()
                back()
                extend()
                grab()
                retract()
                drop()
                retract()
                forward()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                back()
                rotateClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                rotateClockwise()
                forward()
                drop()
                back()
                grab()
                forward()
                drop()
                rotateClockwise()
                rotateClockwise()
                back()
                grab()
                pivotCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                extend()
                pivotCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                retract()
                pivotCounterClockwise()
                rotateClockwise()
                pivotCounterClockwise()
                extend()
                forward()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                pivotClockwise()
                rotateCounterClockwise()
                extend()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                rotateCounterClockwise()
                pivotCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                retract()
                extend()
                extend()
                rotateCounterClockwise()
                back()
                reset()
            }
        }
        )
    }
}
