
solution {
    puzzle = "P067"
    name = "B AA 6T"
    arm(PISTON) {
        number = 1
        position = 2 to 0
        rotation = 0
        size = 1
    }
    glyph(MULTI_BONDER) {
        position = 1 to 2
        rotation = 0
    }
    glyph(UNBONDER) {
        position = 2 to 1
        rotation = 0
    }
    glyph(CALCIFICATION) {
        position = 3 to -2
        rotation = 0
    }
    glyph(PROJECTION) {
        position = 2 to -1
        rotation = -3
    }
    io(INPUT) {
        index = 0
        position = 5 to 1
        rotation = 5
    }
    io(INFINITE) {
        index = 0
        position = -12 to 2
        rotation = 0
    }
    io(OUTPUT) {
        index = 1
        position = 2 to 3
        rotation = 0
    }
    io(INPUT) {
        index = 1
        position = 4 to -1
        rotation = 3
    }
    track {
        position = 1 to 0
        positions = listOf(0 to 0, 1 to 0)
    }
    tape {
        parallel(
        {
            sequence(1) {
                grab()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                drop()
                retract()
                grab()
                rotateClockwise()
                extend()
                drop()
                back()
                retract()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                drop()
                retract()
                grab()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                back()
                drop()
                forward()
                extend()
                grab()
                retract()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                back()
                rotateCounterClockwise()
                extend()
                drop()
                rotateClockwise()
                forward()
                grab()
                back()
                retract()
                pivotClockwise()
                drop()
                extend()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                drop()
                forward()
                rotateClockwise()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateClockwise()
                back()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                drop()
                extend()
                grab()
                retract()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                retract()
                forward()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                forward()
                grab()
                back()
                pivotClockwise()
                retract()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                back()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                forward()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                back()
                extend()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                extend()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                rotateClockwise()
                pivotClockwise()
                pivotCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                forward()
                rotateClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                pivotClockwise()
                drop()
                extend()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                back()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                drop()
                rotateClockwise()
                forward()
                grab()
                pivotClockwise()
                pivotClockwise()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                grab()
                retract()
                drop()
                retract()
                grab()
                back()
                drop()
                extend()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                pivotClockwise()
                retract()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                back()
                drop()
                extend()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                drop()
                rotateClockwise()
                back()
                grab()
                pivotCounterClockwise()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                drop()
                forward()
                extend()
                grab()
                back()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                rotateClockwise()
                pivotClockwise()
                pivotCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                forward()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                retract()
                reset()
                grab()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                drop()
                retract()
                grab()
                rotateClockwise()
                extend()
                drop()
                back()
                retract()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                drop()
                retract()
                grab()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                back()
                drop()
                forward()
                extend()
                grab()
                retract()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                back()
                rotateCounterClockwise()
                extend()
                drop()
                rotateClockwise()
                forward()
                grab()
                back()
                retract()
                pivotClockwise()
                drop()
                extend()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                drop()
                forward()
                rotateClockwise()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateClockwise()
                back()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                drop()
                extend()
                grab()
                retract()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                retract()
                forward()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                forward()
                grab()
                back()
                pivotClockwise()
                retract()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                back()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                forward()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                back()
                extend()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                extend()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                rotateClockwise()
                pivotClockwise()
                pivotCounterClockwise()
                drop()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                forward()
                drop()
                back()
                grab()
                forward()
                drop()
                retract()
                back()
                grab()
                forward()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                forward()
                extend()
                extend()
                grab()
                retract()
                retract()
                pivotClockwise()
                drop()
                extend()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                back()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                drop()
                rotateClockwise()
                forward()
                grab()
                pivotClockwise()
                pivotClockwise()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                retract()
                drop()
                retract()
                grab()
                back()
                drop()
                extend()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                pivotClockwise()
                retract()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                rotateClockwise()
                pivotClockwise()
                pivotCounterClockwise()
                rotateCounterClockwise()
                extend()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                drop()
                back()
                grab()
                forward()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                extend()
                grab()
                back()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                rotateCounterClockwise()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                forward()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                retract()
                reset()
                grab()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                drop()
                retract()
                grab()
                rotateClockwise()
                extend()
                drop()
                back()
                retract()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                drop()
                retract()
                grab()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                back()
                drop()
                forward()
                extend()
                grab()
                retract()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                back()
                rotateCounterClockwise()
                extend()
                drop()
                rotateClockwise()
                forward()
                grab()
                back()
                retract()
                pivotClockwise()
                drop()
                extend()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                drop()
                forward()
                rotateClockwise()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateClockwise()
                back()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                drop()
                extend()
                grab()
                retract()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                retract()
                forward()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                forward()
                grab()
                back()
                pivotClockwise()
                retract()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                back()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                forward()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                back()
                extend()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                extend()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                rotateClockwise()
                pivotClockwise()
                pivotCounterClockwise()
                drop()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                forward()
                drop()
                back()
                grab()
                forward()
                drop()
                retract()
                back()
                grab()
                forward()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                forward()
                extend()
                extend()
                grab()
                retract()
                retract()
                pivotClockwise()
                drop()
                extend()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                back()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                drop()
                rotateClockwise()
                forward()
                grab()
                pivotClockwise()
                pivotClockwise()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                retract()
                drop()
                retract()
                grab()
                back()
                drop()
                extend()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                pivotClockwise()
                retract()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                rotateClockwise()
                pivotClockwise()
                pivotCounterClockwise()
                rotateCounterClockwise()
                extend()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                drop()
                back()
                grab()
                forward()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                extend()
                grab()
                back()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                rotateCounterClockwise()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                forward()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                retract()
                reset()
                grab()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                drop()
                retract()
                grab()
                rotateClockwise()
                extend()
                drop()
                back()
                retract()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                drop()
                retract()
                grab()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                back()
                drop()
                forward()
                extend()
                grab()
                retract()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                back()
                rotateCounterClockwise()
                extend()
                drop()
                rotateClockwise()
                forward()
                grab()
                back()
                retract()
                pivotClockwise()
                drop()
                extend()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                drop()
                forward()
                rotateClockwise()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateClockwise()
                back()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                drop()
                extend()
                grab()
                retract()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                retract()
                forward()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                forward()
                grab()
                back()
                pivotClockwise()
                retract()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                back()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                forward()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                back()
                extend()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                extend()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                rotateClockwise()
                pivotClockwise()
                pivotCounterClockwise()
                drop()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                forward()
                drop()
                back()
                grab()
                forward()
                drop()
                retract()
                back()
                grab()
                forward()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                forward()
                extend()
                extend()
                grab()
                retract()
                retract()
                pivotClockwise()
                drop()
                extend()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                back()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                drop()
                rotateClockwise()
                forward()
                grab()
                pivotClockwise()
                pivotClockwise()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                retract()
                drop()
                retract()
                grab()
                back()
                drop()
                extend()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                pivotClockwise()
                retract()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                rotateClockwise()
                pivotClockwise()
                pivotCounterClockwise()
                rotateCounterClockwise()
                extend()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                drop()
                back()
                grab()
                forward()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                extend()
                grab()
                back()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                rotateCounterClockwise()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                forward()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                retract()
                reset()
                grab()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                drop()
                retract()
                grab()
                rotateClockwise()
                extend()
                drop()
                back()
                retract()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                drop()
                retract()
                grab()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                back()
                drop()
                forward()
                extend()
                grab()
                retract()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                back()
                rotateCounterClockwise()
                extend()
                drop()
                rotateClockwise()
                forward()
                grab()
                back()
                retract()
                pivotClockwise()
                drop()
                extend()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                drop()
                forward()
                rotateClockwise()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateClockwise()
                back()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                drop()
                extend()
                grab()
                retract()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                retract()
                forward()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                forward()
                grab()
                back()
                pivotClockwise()
                retract()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                back()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                forward()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                back()
                extend()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                extend()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                rotateClockwise()
                pivotClockwise()
                pivotCounterClockwise()
                drop()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                forward()
                drop()
                back()
                grab()
                forward()
                drop()
                retract()
                back()
                grab()
                forward()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                forward()
                extend()
                extend()
                grab()
                retract()
                retract()
                pivotClockwise()
                drop()
                extend()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                back()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                drop()
                rotateClockwise()
                forward()
                grab()
                pivotClockwise()
                pivotClockwise()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                retract()
                drop()
                retract()
                grab()
                back()
                drop()
                extend()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                pivotClockwise()
                retract()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                rotateClockwise()
                pivotClockwise()
                pivotCounterClockwise()
                rotateCounterClockwise()
                extend()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                drop()
                back()
                grab()
                forward()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                extend()
                grab()
                back()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                rotateCounterClockwise()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                forward()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                retract()
                reset()
                grab()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                forward()
                extend()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                rotateCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                drop()
                retract()
                grab()
                rotateClockwise()
                extend()
                drop()
                back()
                retract()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                forward()
                pivotClockwise()
                drop()
                retract()
                grab()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                back()
                drop()
                forward()
                extend()
                grab()
                retract()
                drop()
                back()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                back()
                rotateCounterClockwise()
                extend()
                drop()
                rotateClockwise()
                forward()
                grab()
                back()
                retract()
                pivotClockwise()
                drop()
                extend()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                drop()
                forward()
                rotateClockwise()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                retract()
                retract()
                forward()
                rotateCounterClockwise()
                grab()
                extend()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateClockwise()
                back()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                drop()
                extend()
                grab()
                retract()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                drop()
                retract()
                retract()
                forward()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                forward()
                grab()
                back()
                pivotClockwise()
                retract()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                back()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                forward()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                back()
                extend()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                extend()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                rotateClockwise()
                pivotClockwise()
                pivotCounterClockwise()
                drop()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                back()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                forward()
                drop()
                back()
                grab()
                forward()
                drop()
                retract()
                back()
                grab()
                forward()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                forward()
                extend()
                extend()
                grab()
                retract()
                retract()
                pivotClockwise()
                drop()
                extend()
                grab()
                pivotClockwise()
                pivotClockwise()
                drop()
                retract()
                grab()
                pivotCounterClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                back()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                drop()
                rotateClockwise()
                forward()
                grab()
                pivotClockwise()
                pivotClockwise()
                back()
                pivotCounterClockwise()
                drop()
                extend()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                retract()
                drop()
                retract()
                grab()
                back()
                drop()
                extend()
                grab()
                pivotClockwise()
                forward()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                back()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                pivotClockwise()
                retract()
                forward()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                forward()
                drop()
                back()
                grab()
                forward()
                back()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                grab()
                back()
                rotateCounterClockwise()
                extend()
                rotateCounterClockwise()
                drop()
                retract()
                retract()
                grab()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                back()
                rotateClockwise()
                extend()
                rotateClockwise()
                forward()
                pivotClockwise()
                extend()
                reset()
            }
        }
        )
    }
}
