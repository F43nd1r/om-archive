
solution {
    puzzle = "P095"
    name = "AC"
    arm(PISTON) {
        number = 1
        position = 2 to 1
        rotation = 3
        size = 1
    }
    arm(PISTON) {
        number = 2
        position = 3 to -2
        rotation = 2
        size = 3
    }
    glyph(UNBONDER) {
        position = 1 to 0
        rotation = 0
    }
    glyph(PROJECTION) {
        position = 2 to -1
        rotation = -3
    }
    glyph(PURIFICATION) {
        position = 4 to -1
        rotation = 2
    }
    io(INPUT) {
        index = 0
        position = 0 to 1
        rotation = 0
    }
    io(OUTPUT) {
        index = 0
        position = 1 to -2
        rotation = 0
    }
    tape {
        parallel(
        {
            sequence(2) {
                wait(1)
                retract()
                grab()
                retract()
                rotateCounterClockwise()
                pivotClockwise()
                extend()
                drop()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                wait(1)
                grab()
                extend()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                pivotClockwise()
                drop()
                retract()
                rotateClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                pivotClockwise()
                extend()
                drop()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                wait(1)
                extend()
                drop()
                retract()
                grab()
                wait(6)
                extend()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                pivotClockwise()
                drop()
                retract()
                rotateClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                pivotClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                wait(1)
                extend()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                pivotClockwise()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                extend()
                drop()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                wait(1)
                extend()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                drop()
                extend()
                grab()
                wait(2)
                retract()
                rotateCounterClockwise()
                pivotClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                pivotClockwise()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                wait(1)
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                wait(3)
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                drop()
                extend()
                grab()
                wait(1)
                retract()
                rotateCounterClockwise()
                pivotClockwise()
                extend()
                drop()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                reset()
                wait(2)
                grab()
                retract()
                drop()
                retract()
                rotateClockwise()
                grab()
                wait(3)
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                extend()
                drop()
                retract()
                grab()
                extend()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                pivotClockwise()
                drop()
                retract()
                rotateClockwise()
                extend()
                grab()
                wait(7)
                retract()
                rotateCounterClockwise()
                pivotClockwise()
                extend()
                drop()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                drop()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                reset()
                wait(2)
                grab()
                retract()
                drop()
                retract()
                rotateClockwise()
                grab()
                wait(3)
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                extend()
                drop()
                retract()
                grab()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                reset()
                wait(2)
                grab()
                retract()
                retract()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                pivotClockwise()
                drop()
                wait(8)
                extendTape()
            }
        }
        , 
        {
            sequence(1) {
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                wait(10)
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                wait(2)
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                wait(3)
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                wait(11)
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                extend()
                drop()
                retract()
                retract()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                extend()
                extend()
                grab()
                retract()
                retract()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                wait(3)
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                reset()
                wait(2)
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                wait(11)
                grab()
                extend()
                drop()
                retract()
                retract()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                wait(3)
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                extend()
                grab()
                retract()
                wait(4)
                extend()
                drop()
                retract()
                retract()
                grab()
                wait(1)
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                extend()
                grab()
                extend()
                drop()
                retract()
                retract()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                extend()
                wait(1)
                grab()
                retract()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                extend()
                wait(2)
                grab()
                retract()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                wait(2)
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                rotateClockwise()
                wait(1)
                extend()
                drop()
                wait(5)
                grab()
                extend()
                drop()
                retract()
                retract()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                wait(2)
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                extend()
                extend()
                grab()
                retract()
                wait(2)
                extend()
                drop()
                retract()
                retract()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                rotateClockwise()
                wait(1)
                extend()
                extend()
                drop()
                retract()
                retract()
                wait(3)
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                wait(2)
                grab()
                wait(4)
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                wait(3)
                grab()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                extend()
                extend()
                grab()
                wait(6)
                retract()
                drop()
                retract()
                rotateClockwise()
                grab()
                pivotCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                extend()
                grab()
                retract()
                rotateCounterClockwise()
                wait(1)
                rotateClockwise()
                extend()
                extend()
                drop()
                retract()
                retract()
                wait(3)
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                wait(2)
                grab()
                wait(4)
                rotateCounterClockwise()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                wait(3)
                grab()
                extend()
                drop()
                retract()
                rotateClockwise()
                grab()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                extend()
                drop()
                retract()
                rotateClockwise()
                wait(10)
                grab()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                extend()
                extend()
                grab()
                retract()
            }
        }
        )
    }
}
