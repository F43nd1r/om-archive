
solution {
    puzzle = "P096"
    name = "B GC 6T"
    arm(ARM1) {
        number = 1
        position = -1 to 1
        rotation = 1
        size = 2
    }
    glyph(UNBONDER) {
        position = 1 to 2
        rotation = -2
    }
    glyph(DISPOSAL) {
        position = 2 to -1
        rotation = 0
    }
    io(INPUT) {
        index = 0
        position = -2 to 4
        rotation = -6
    }
    io(OUTPUT) {
        index = 0
        position = -1 to 0
        rotation = -6
    }
    io(OUTPUT) {
        index = 1
        position = 0 to 4
        rotation = 4
    }
    io(OUTPUT) {
        index = 2
        position = 1 to -2
        rotation = -3
    }
    io(OUTPUT) {
        index = 3
        position = 2 to 2
        rotation = 1
    }
    track {
        position = 0 to 1
        positions = listOf(0 to 0, -1 to 0)
    }
    tape {
        parallel(
        {
            sequence(1) {
                grab()
                rotateClockwise()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                reset()
                wait(2)
                repeat()
                wait(31)
                repeat()
                wait(31)
                repeat()
                wait(31)
                repeat()
                wait(31)
                repeat()
                wait(31)
                grab()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                repeat()
                wait(38)
                repeat()
                wait(38)
                repeat()
                wait(38)
                repeat()
                wait(38)
                grab()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                drop()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                pivotClockwise()
                drop()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                forward()
                pivotCounterClockwise()
                pivotCounterClockwise()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                rotateClockwise()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                pivotClockwise()
                forward()
                drop()
                back()
                grab()
                rotateClockwise()
                rotateClockwise()
                pivotClockwise()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                grab()
                rotateClockwise()
                back()
                reset()
            }
        }
        )
    }
}
