
solution {
    puzzle = "w2450511665"
    name = "B GA 6T"
    arm(ARM1) {
        number = 1
        position = -1 to -2
        rotation = 1
        size = 2
    }
    glyph(BONDER) {
        position = -2 to -3
        rotation = -3
    }
    glyph(UNBONDER) {
        position = 2 to -5
        rotation = -4
    }
    glyph(ANIMISMUS) {
        position = 0 to -4
        rotation = -8
    }
    glyph(DISPOSAL) {
        position = 2 to -3
        rotation = 0
    }
    glyph(EQUILIBRIUM) {
        position = -3 to -1
        rotation = 0
    }
    io(INPUT) {
        index = 0
        position = -1 to 0
        rotation = -6
    }
    io(OUTPUT) {
        index = 0
        position = -2 to -1
        rotation = -6
    }
    track {
        position = 0 to -2
        positions = listOf(-1 to -1, -1 to 0, 0 to 0, 0 to -1)
    }
    tape {
        parallel(
        {
            sequence(1) {
                grab()
                back()
                pivotClockwise()
                rotateClockwise()
                drop()
                forward()
                grab()
                back()
                forward()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                rotateCounterClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                back()
                grab()
                pivotCounterClockwise()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                forward()
                grab()
                back()
                rotateCounterClockwise()
                forward()
                drop()
                forward()
                forward()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                forward()
                drop()
                forward()
                grab()
                forward()
                forward()
                rotateClockwise()
                forward()
                forward()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                back()
                back()
                drop()
                back()
                rotateClockwise()
                forward()
                grab()
                back()
                rotateClockwise()
                drop()
                back()
                grab()
                forward()
                rotateClockwise()
                forward()
                rotateClockwise()
                forward()
                forward()
                drop()
                back()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                forward()
                reset()
                grab()
                back()
                pivotClockwise()
                rotateClockwise()
                drop()
                forward()
                grab()
                back()
                forward()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                rotateCounterClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                grab()
                rotateCounterClockwise()
                drop()
                back()
                grab()
                pivotCounterClockwise()
                rotateCounterClockwise()
                back()
                back()
                drop()
                forward()
                forward()
                grab()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                forward()
                grab()
                back()
                rotateCounterClockwise()
                forward()
                drop()
                forward()
                forward()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                forward()
                drop()
                forward()
                grab()
                forward()
                forward()
                rotateClockwise()
                forward()
                forward()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                back()
                back()
                drop()
                forward()
                rotateClockwise()
                back()
                grab()
                back()
                rotateClockwise()
                drop()
                back()
                grab()
                forward()
                rotateClockwise()
                back()
                drop()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                forward()
                reset()
                grab()
                back()
                pivotClockwise()
                rotateClockwise()
                drop()
                forward()
                grab()
                back()
                forward()
                rotateClockwise()
                drop()
                rotateClockwise()
                forward()
                grab()
                pivotClockwise()
                back()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                rotateCounterClockwise()
                forward()
                drop()
                back()
                back()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotCounterClockwise()
                forward()
                drop()
                back()
                grab()
                pivotCounterClockwise()
                rotateClockwise()
                forward()
                rotateClockwise()
                pivotClockwise()
                drop()
                back()
                grab()
                back()
                back()
                back()
                rotateCounterClockwise()
                forward()
                drop()
                back()
                rotateCounterClockwise()
                forward()
                grab()
                back()
                rotateCounterClockwise()
                forward()
                drop()
                back()
                back()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotCounterClockwise()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                grab()
                back()
                back()
                rotateCounterClockwise()
                forward()
                drop()
                back()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                back()
                rotateClockwise()
                drop()
                forward()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateClockwise()
                drop()
                forward()
                grab()
                back()
                drop()
                forward()
                rotateClockwise()
                rotateClockwise()
                forward()
                reset()
                repeat()
                wait(289)
                repeat()
                wait(289)
                repeat()
                wait(289)
                repeat()
                wait(289)
                grab()
                back()
                pivotClockwise()
                rotateClockwise()
                drop()
                forward()
                grab()
                back()
                forward()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                rotateCounterClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                drop()
                back()
                grab()
                pivotCounterClockwise()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                forward()
                grab()
                back()
                rotateCounterClockwise()
                forward()
                drop()
                forward()
                forward()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                forward()
                drop()
                forward()
                grab()
                forward()
                forward()
                rotateClockwise()
                forward()
                forward()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                back()
                back()
                drop()
                back()
                rotateClockwise()
                forward()
                grab()
                back()
                rotateClockwise()
                drop()
                back()
                grab()
                forward()
                rotateClockwise()
                forward()
                rotateClockwise()
                forward()
                forward()
                drop()
                back()
                rotateClockwise()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                forward()
                reset()
                grab()
                back()
                pivotClockwise()
                rotateClockwise()
                drop()
                forward()
                grab()
                back()
                forward()
                rotateClockwise()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                rotateCounterClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                back()
                grab()
                rotateCounterClockwise()
                drop()
                back()
                grab()
                pivotCounterClockwise()
                rotateCounterClockwise()
                back()
                back()
                drop()
                forward()
                forward()
                grab()
                rotateClockwise()
                forward()
                drop()
                rotateCounterClockwise()
                forward()
                grab()
                back()
                rotateCounterClockwise()
                forward()
                drop()
                forward()
                forward()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                forward()
                drop()
                forward()
                grab()
                forward()
                forward()
                rotateClockwise()
                forward()
                forward()
                drop()
                back()
                rotateCounterClockwise()
                grab()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                back()
                back()
                drop()
                forward()
                rotateClockwise()
                back()
                grab()
                back()
                rotateClockwise()
                drop()
                back()
                grab()
                forward()
                rotateClockwise()
                back()
                drop()
                forward()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                rotateCounterClockwise()
                back()
                drop()
                rotateCounterClockwise()
                forward()
                reset()
                grab()
                back()
                pivotClockwise()
                rotateClockwise()
                drop()
                forward()
                grab()
                back()
                forward()
                rotateClockwise()
                drop()
                rotateClockwise()
                forward()
                grab()
                pivotClockwise()
                back()
                drop()
                rotateCounterClockwise()
                forward()
                forward()
                grab()
                back()
                rotateCounterClockwise()
                forward()
                drop()
                back()
                back()
                rotateClockwise()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotCounterClockwise()
                forward()
                drop()
                back()
                grab()
                pivotCounterClockwise()
                rotateClockwise()
                forward()
                rotateClockwise()
                pivotClockwise()
                drop()
                back()
                grab()
                back()
                back()
                back()
                rotateCounterClockwise()
                forward()
                drop()
                back()
                rotateCounterClockwise()
                forward()
                grab()
                back()
                rotateCounterClockwise()
                drop()
                back()
                rotateClockwise()
                grab()
                rotateCounterClockwise()
                pivotCounterClockwise()
                drop()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                forward()
                grab()
                back()
                back()
                rotateCounterClockwise()
                forward()
                drop()
                back()
                back()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                back()
                rotateClockwise()
                drop()
                forward()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateClockwise()
                drop()
                forward()
                grab()
                back()
                drop()
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                forward()
                reset()
            }
        }
        )
    }
}
