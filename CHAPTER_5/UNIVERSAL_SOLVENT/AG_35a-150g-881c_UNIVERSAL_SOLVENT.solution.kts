
solution {
    puzzle = "P043"
    name = "B AG 6T"
    arm(VAN_BERLOS_WHEEL) {
        number = 1
        position = -2 to -4
        rotation = -2
        size = 1
    }
    arm(PISTON) {
        number = 2
        position = 4 to -6
        rotation = -3
        size = 1
    }
    glyph(BONDER) {
        position = 2 to -4
        rotation = -8
    }
    glyph(DUPLICATION) {
        position = -1 to -4
        rotation = -6
    }
    glyph(PURIFICATION) {
        position = 4 to -4
        rotation = -10
    }
    glyph(ANIMISMUS) {
        position = 1 to -6
        rotation = -11
    }
    glyph(EQUILIBRIUM) {
        position = 2 to -2
        rotation = 0
    }
    io(OUTPUT) {
        index = 0
        position = 0 to -2
        rotation = -4
    }
    io(INPUT) {
        index = 2
        position = 3 to -6
        rotation = 0
    }
    io(INPUT) {
        index = 4
        position = 4 to -5
        rotation = 0
    }
    track {
        position = 3 to -5
        positions = listOf(0 to 0, 1 to -1)
    }
    tape {
        parallel(
        {
            sequence(2) {
                grab()
                extend()
                extend()
                reset()
                wait(2)
                grab()
                back()
                extend()
                drop()
                extend()
                grab()
                retract()
                retract()
                reset()
                wait(1)
                rotateClockwise()
                rotateClockwise()
                grab()
                extend()
                drop()
                retract()
                grab()
                back()
                extend()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                reset()
                wait(2)
                extend()
                grab()
                back()
                retract()
                pivotCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                drop()
                extend()
                grab()
                pivotClockwise()
                retract()
                pivotClockwise()
                reset()
                wait(2)
                grab()
                back()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                pivotClockwise()
                reset()
                wait(2)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                reset()
                wait(1)
                grab()
                extend()
                extend()
                reset()
                wait(2)
                grab()
                extend()
                back()
                drop()
                rotateClockwise()
                grab()
                extend()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                retract()
                reset()
                wait(1)
                rotateClockwise()
                rotateClockwise()
                grab()
                extend()
                drop()
                retract()
                grab()
                back()
                extend()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                reset()
                wait(2)
                extend()
                grab()
                back()
                retract()
                pivotCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                drop()
                extend()
                grab()
                pivotClockwise()
                retract()
                pivotClockwise()
                reset()
                wait(2)
                grab()
                back()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                pivotClockwise()
                reset()
                wait(2)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                reset()
                wait(1)
                grab()
                extend()
                extend()
                reset()
                wait(2)
                grab()
                extend()
                back()
                drop()
                rotateClockwise()
                grab()
                extend()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                retract()
                reset()
                wait(1)
                rotateClockwise()
                rotateClockwise()
                grab()
                extend()
                drop()
                retract()
                grab()
                back()
                extend()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                reset()
                wait(2)
                extend()
                grab()
                back()
                retract()
                pivotCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                drop()
                extend()
                grab()
                pivotClockwise()
                retract()
                pivotClockwise()
                reset()
                wait(2)
                grab()
                back()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                pivotClockwise()
                reset()
                wait(2)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                reset()
                wait(1)
                grab()
                extend()
                back()
                reset()
                wait(2)
                grab()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                retract()
                reset()
                wait(1)
                rotateClockwise()
                rotateClockwise()
                grab()
                extend()
                drop()
                retract()
                grab()
                back()
                extend()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                reset()
                wait(2)
                extend()
                grab()
                back()
                retract()
                pivotCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                drop()
                extend()
                grab()
                pivotClockwise()
                retract()
                pivotClockwise()
                reset()
                wait(2)
                grab()
                back()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                pivotClockwise()
                reset()
                wait(2)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                reset()
                wait(1)
                grab()
                extend()
                back()
                reset()
                wait(2)
                grab()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                retract()
                reset()
                wait(1)
                rotateClockwise()
                rotateClockwise()
                grab()
                extend()
                drop()
                retract()
                grab()
                back()
                extend()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                reset()
                wait(2)
                extend()
                grab()
                back()
                retract()
                pivotCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                drop()
                extend()
                grab()
                pivotClockwise()
                retract()
                pivotClockwise()
                reset()
                wait(2)
                grab()
                back()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                pivotClockwise()
                reset()
                wait(2)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                reset()
                wait(1)
                grab()
                extend()
                back()
                reset()
                wait(2)
                grab()
                extend()
                extend()
                drop()
                rotateClockwise()
                grab()
                back()
                drop()
                rotateCounterClockwise()
                grab()
                retract()
                retract()
                reset()
                wait(1)
                rotateClockwise()
                rotateClockwise()
                grab()
                extend()
                drop()
                retract()
                grab()
                back()
                extend()
                drop()
                retract()
                grab()
                rotateCounterClockwise()
                pivotClockwise()
                pivotClockwise()
                pivotClockwise()
                reset()
                wait(2)
                extend()
                grab()
                back()
                retract()
                pivotCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                drop()
                rotateClockwise()
                grab()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                pivotClockwise()
                pivotClockwise()
                rotateCounterClockwise()
                reset()
                wait(1)
                grab()
                rotateClockwise()
                back()
                drop()
                extend()
                grab()
                pivotClockwise()
                retract()
                pivotClockwise()
                reset()
                wait(2)
                grab()
                back()
                extend()
                drop()
                rotateClockwise()
                grab()
                retract()
                pivotClockwise()
                reset()
                wait(2)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                pivotClockwise()
                reset()
                wait(3)
                grab()
                back()
                drop()
                rotateClockwise()
                grab()
                extend()
                reset()
            }
        }
        , 
        {
            sequence(1) {
                wait(67)
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                wait(8)
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                wait(5)
                rotateCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                wait(66)
                reset()
                wait(58)
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                wait(8)
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                wait(5)
                rotateCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                wait(66)
                reset()
                wait(58)
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                wait(8)
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                wait(5)
                rotateCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                wait(66)
                reset()
                wait(58)
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                wait(8)
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                wait(5)
                rotateCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                wait(66)
                reset()
                wait(58)
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                wait(8)
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                wait(5)
                rotateCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                wait(66)
                reset()
                wait(58)
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                wait(8)
                rotateCounterClockwise()
                rotateCounterClockwise()
                rotateCounterClockwise()
                wait(5)
                rotateCounterClockwise()
                rotateClockwise()
                rotateClockwise()
                wait(57)
                reset()
            }
        }
        )
    }
}
