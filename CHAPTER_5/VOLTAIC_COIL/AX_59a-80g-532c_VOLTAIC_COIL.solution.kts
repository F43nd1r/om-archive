
solution {
    puzzle = "P039"
    name = "B AX 6T"
    arm(PISTON) {
        number = 1
        position = -1 to -3
        rotation = 1
        size = 1
    }
    glyph(BONDER) {
        position = 0 to -2
        rotation = -1
    }
    glyph(BONDER) {
        position = 1 to -4
        rotation = -2
    }
    glyph(CALCIFICATION) {
        position = 0 to -3
        rotation = 0
    }
    glyph(EQUILIBRIUM) {
        position = -1 to -1
        rotation = 0
    }
    io(INPUT) {
        index = 0
        position = -1 to -2
        rotation = 0
    }
    io(INFINITE) {
        index = 0
        position = 1 to -2
        rotation = 0
    }
    io(INPUT) {
        index = 2
        position = -1 to 0
        rotation = 0
    }
    track {
        position = -1 to -3
        positions = listOf(0 to 0, 1 to -1)
    }
    tape {
        parallel(
        {
            sequence(1) {
                grab()
                extend()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                back()
                rotateClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                back()
                rotateClockwise()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                forward()
                extend()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                drop()
                retract()
                retract()
                forward()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                reset()
                grab()
                extend()
                rotateClockwise()
                forward()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                back()
                extend()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                back()
                rotateClockwise()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                extend()
                forward()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                drop()
                retract()
                grab()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                back()
                rotateClockwise()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                forward()
                extend()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                drop()
                retract()
                retract()
                forward()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                reset()
                grab()
                extend()
                rotateClockwise()
                forward()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                back()
                extend()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                back()
                rotateClockwise()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                extend()
                forward()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                drop()
                retract()
                grab()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                back()
                rotateClockwise()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                forward()
                extend()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                drop()
                retract()
                retract()
                forward()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                reset()
                grab()
                extend()
                rotateClockwise()
                forward()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                back()
                extend()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                back()
                rotateClockwise()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                extend()
                forward()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                drop()
                retract()
                grab()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                back()
                rotateClockwise()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                forward()
                extend()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                drop()
                retract()
                retract()
                forward()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                reset()
                grab()
                extend()
                rotateClockwise()
                forward()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                back()
                extend()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                back()
                rotateClockwise()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                extend()
                forward()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                drop()
                retract()
                grab()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                back()
                rotateClockwise()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                forward()
                extend()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                drop()
                retract()
                retract()
                forward()
                grab()
                extend()
                extend()
                drop()
                retract()
                retract()
                rotateCounterClockwise()
                reset()
                grab()
                extend()
                rotateClockwise()
                forward()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                extend()
                grab()
                retract()
                drop()
                rotateCounterClockwise()
                back()
                extend()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                back()
                rotateClockwise()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                extend()
                forward()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                drop()
                retract()
                grab()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateClockwise()
                drop()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                forward()
                back()
                rotateClockwise()
                drop()
                retract()
                rotateCounterClockwise()
                grab()
                rotateClockwise()
                rotateClockwise()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                rotateCounterClockwise()
                grab()
                forward()
                extend()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                drop()
                retract()
                grab()
                extend()
                drop()
                retract()
                rotateCounterClockwise()
                extend()
                grab()
                retract()
                rotateClockwise()
                drop()
                retract()
                reset()
                grab()
                extend()
                forward()
                back()
                drop()
                rotateClockwise()
                extend()
                grab()
                retract()
                extend()
                rotateClockwise()
                forward()
                reset()
            }
        }
        )
    }
}
